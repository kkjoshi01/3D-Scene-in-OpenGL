#version 330 core
layout (location = 0) in vec3 position;

out vec3 texCoords;

uniform mat4 view;
uniform mat4 projection;
uniform mat4 model;

void main() {

    
    texCoords = position;
    //vec4 pos = mat4((mat3(view))) * projection * vec4(position, 1.0);
    vec4 pos = model * projection * vec4(position, 1.0);

    gl_Position = pos.xyww;    

}

//Skybox Vertex Shader modified from: https://learnopengl.com/code_viewer_gh.php?code=src/4.advanced_opengl/6.2.cubemaps_environment_mapping/6.2.skybox.vs